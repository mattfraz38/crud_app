<nav class="navbar navbar-expand-md navbar-light">
  <div class="container-fluid">
    <% if user_signed_in? %>
      <%= link_to user_path(current_user), class: "navbar-brand" do%>
        <%= image_pack_tag "nav-logo.png", class:"ms-3 w-25"%>
      <% end %>
    <% else %>
      <%= link_to root_path, class: "navbar-brand" do%>
        <%= image_pack_tag "nav-logo.png", class:"ms-3 w-25"%>
      <% end %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link" %>
        </li>      
        <li class="nav-item">
          <%= link_to "Articles", articles_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Users", users_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= current_user.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenuLink">
              <li>
                <%= link_to "My Page", user_path(current_user), class: "dropdown-item" %>
              </li>
              <li>
                <%= link_to "My Account", edit_user_registration_path(current_user), class: "dropdown-item" %>
              </li>
              
              <li>
                <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
          </li>        
        <% end %>
      </ul>
    </div>
  </div>
</nav>